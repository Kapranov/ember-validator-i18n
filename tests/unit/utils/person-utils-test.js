import personUtils from 'ember-validator-i18n/utils/person-utils';
import { module, test } from 'qunit';

module('Unit | Utility | person utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = personUtils();
  assert.ok(result);
});
